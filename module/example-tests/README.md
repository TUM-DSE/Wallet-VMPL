## Known Test Failures

* ...

## Tests to Add

* trustlet_converge2 without memory channel and > 1 iteration
* minimalist, single invokation (run.py, trustlet.c)
* nice and minimal chaining invocation (run.py, trustlet.c, default_loop(), simple init)
* minimalist parallelism and long-running test
* TODO remove hardcoded nix paths in example-*/Makefile's
